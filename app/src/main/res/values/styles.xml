<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#ddd</item>
        <item name="colorPrimaryDark">#ddd</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.Dialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="ActivityAnimation" mce_bogus="1" parent="android:Animation">
        <item name="@android:activityOpenEnterAnimation">@anim/activity_open_enter</item>
        <item name="@android:activityOpenExitAnimation">@anim/activity_open_exit</item>
        <item name="@android:activityCloseEnterAnimation">@anim/activity_close_enter</item>
        <item name="@android:activityCloseExitAnimation">@anim/activity_close_exit</item>
    </style>

    <style name="DialogAnimation" mce_bogus="1" parent="android:Animation">
        <item name="@android:activityOpenEnterAnimation">@anim/dialog_open_enter</item>
        <item name="@android:activityOpenExitAnimation">@anim/dialog_open_exit</item>
        <item name="@android:activityCloseEnterAnimation">@anim/dialog_close_enter</item>
        <item name="@android:activityCloseExitAnimation">@anim/dialog_close_exit</item>
    </style>
</resources>
